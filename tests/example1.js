import assert from 'assert';
import parser from '../dist/bundle';
import report1 from './reports-examples/report.7.report';

describe('multiply', () => {
  it('returns 0 when either argument is 0', () => {
    console.log(parser, report1);
    assert.equal(1, 1);
  });
});
